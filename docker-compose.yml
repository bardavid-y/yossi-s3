services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app          # Maps local folder to container (Hot Reloading)
      - /app/node_modules # Prevents local node_modules from overwriting container's
    environment:
      - NODE_ENV=development